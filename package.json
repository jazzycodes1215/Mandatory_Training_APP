{
  "name": "mandatory_training_app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm start --prefix mandatory-training ${start} && npm run --prefix back-end ${start}",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "native-notify": "^3.0.6"
  }
}
